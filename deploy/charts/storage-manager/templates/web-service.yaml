apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.web.service.name | quote }}
  namespace: {{ include "storage-manager.namespace" . | quote }}
  labels:
    {{- include "storage-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type | quote }}
  selector:
    {{- include "storage-manager.webSelectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: {{ .Values.web.service.targetPort }}
